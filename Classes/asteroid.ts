export class Asteroid {

    angle: number;
    x: number;
    y: number;
    speed: number;
    radius: number;

    constructor(angle, x, y, speed, radius) {
        this.angle = angle;
        this.x = x;
        this.y = y;
        this.speed = speed;
        this.radius = radius;
    }
}